public class BasicProperty {
    public static void poo(){
        Map<Id, account> acc_list = new Map<Id, account>([select id from account]);
        List<string> acc = new List<string>();
        Integer f = 10;
        for(Integer s=0;s>10;s++)
        {
            system.debug('The value is' +s);
        }
        system.debug('The value is valied till'+acc_list );
        
    }
}